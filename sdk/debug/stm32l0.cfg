# OpenOCD configuration for Core.ST.L0 tiles (STM32L011, ST-Link)
# Note: Cortex-M0+ does NOT have ITM/SWO — no trace output

source [find interface/stlink.cfg]
transport select hla_swd

source [find target/stm32l0.cfg]

adapter speed 4000

init
reset halt

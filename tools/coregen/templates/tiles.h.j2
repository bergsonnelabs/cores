/**
 * tiles.h — Tile driver handles and configuration for {{ project.name }}
 *
 * Initially generated by coregen. The section between coregen markers
 * is kept in sync with project.json automatically — add your own
 * tile-related defines outside the markers.
 */

#ifndef TILES_H
#define TILES_H

/* ---- coregen:begin ---- */
#include "tiles_hal.h"
#include "tiles_hal_core.h"
{% for header in tile_driver_headers %}
#include "{{ header }}"
{% endfor %}

/* Tile HAL handles (one per bus used by tiles) */
{% for bus in tile_hal_buses %}
tiles_hal_t {{ bus.hal_handle }};
{% endfor %}

/* Tile instance handles */
{% for tile in tiles_config %}
tile_t {{ tile.handle }};
{% endfor %}
/* ---- coregen:end ---- */

#endif /* TILES_H */

/**
 * core.h — Single include for {{ tile_name }} projects
 *
 * AUTO-GENERATED by coregen from {{ source_file }}{% if project_file %} + {{ project_file }}{% endif %}
 *
 * Include this one file in main.c to get everything:
 *   - Clock, pin, and driver initialization (tile_init)
 *   - LED and board defines (tile_board)
 *   - Clock configuration (SYSCLK_HZ, etc.)
 *   - LL essentials (GPIO, RCC, SysTick, delay)
 *   - Tile driver handles and APIs (if tiles declared)
 *
 * DO NOT EDIT — regenerate by rebuilding the project.
 */

#ifndef CORE_H
#define CORE_H

/* ---- Generated project headers ---- */
#include "tile_init.h"
#include "tile_board.h"
#include "tile_config.h"

/* ---- SDK essentials ---- */
#include "ll_rcc.h"
#include "ll_gpio.h"
#include "ll_systick.h"
#include "hal_led.h"

#endif /* CORE_H */

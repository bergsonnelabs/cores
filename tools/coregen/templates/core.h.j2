/**
 * core.h — Single include for {{ tile_name }} projects
 *
 * AUTO-GENERATED by coregen from {{ source_file }}{{ " + " + config_file if config_file else "" }}
 *
 * Include this one file in main.c to get everything:
 *   - Clock, pad, and driver initialization (core_init)
 *   - LED and board defines (core_board)
 *   - Clock configuration (SYSCLK_HZ, etc.)
 *   - The core_ API (pads, ADC, interrupts, timing, etc.)
 *   - Tile driver init recipe (see tiles.h for handles)
 *
 * DO NOT EDIT — regenerate by rebuilding the project.
 */

#ifndef CORE_H
#define CORE_H

/* ---- Generated project headers ---- */
#include "core_init.h"
#include "core_board.h"
#include "core_config.h"

/* ---- SDK essentials ---- */
#include "ll_rcc.h"
#include "ll_gpio.h"
#include "ll_systick.h"

/* ---- core_ API layer ---- */
#include "core_led.h"
#include "core_gpio.h"
#include "core_timing.h"
{% if usb_enabled %}
#include "core_usb.h"
{% endif %}
{% if dac_enabled %}
#include "core_dac.h"
{% endif %}
{% if i2c_buses %}
#include "core_i2c.h"
{% endif %}
{% if spi_buses %}
#include "core_spi.h"
{% endif %}
{% if tiles_config %}
#include "core_tiles.h"
{% endif %}

#endif /* CORE_H */
